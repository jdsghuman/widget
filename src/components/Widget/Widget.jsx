import React from 'react'
import styles from './Widget.module.scss'

const Widget = ({ logo, name, organization, themeColor }) => {
  return <div className={styles.container}>Widget</div>
}

export default Widget
